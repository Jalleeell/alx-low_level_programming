#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A cprogram that prints single digits from 0 to 9
 *
 * Return:  (success)
*/
int main(void)
{
	int i;

	for (i = 0 ; i <= 9 ; i++)
{
	printf("%d", i);
}
	printf("\n");
	return (0);
}
