#include "main.h"

/**
 * _islower -  checks if character is lowercas
 * @c: character to be cheked
 *
 * Return: 1 if c is lowercase, else, returns 0.
 */

int _islower(int c)
{
		if (c >= 97 && c <= 122)

			return (1);


	return (0);
}
