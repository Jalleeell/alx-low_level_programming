#include "main.h"

/**
 * _abs - computes the absolute value of an int
 * @x: int to be checked
 *
 * Return: the absolute value of x
*/

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	return (-x);
}
